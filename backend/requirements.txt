# O suporte a autenticacao por chave de API do Bedrock via variavel de ambiente
# AWS_BEARER_TOKEN_BEDROCK foi introduzido em boto3/botocore 1.39.0
# (verificado: botocore 1.38.46 nao possui o resolvedor de token de ambiente).
#
# O piso subiu de 1.42.97 para 1.43.62 porque langchain-aws 1.6.4 exige
# boto3>=1.43.32. O resolvedor de bearer token continua funcionando.
boto3==1.43.62
python-dotenv==1.2.1

# API
fastapi==0.141.1
uvicorn[standard]==0.52.1

# LangChain — versoes fixadas de proposito. Sem pin, "pip install langchain-aws"
# resolve para o que estiver publicado no dia, e o roteiro da aula deixa de ser
# reproduzivel entre turmas.
langchain-core==1.5.3
langchain-aws==1.6.4

# Postgres. O extra [binary] traz a libpq compilada no wheel: sem ele a
# instalacao exige pg_config e cabecalhos de C na maquina.
psycopg[binary]==3.3.4

# Base vetorial e chunking. As duas exigem langchain-core: langchain-postgres
# aceita <2.0 e langchain-text-splitters exige >=1.2.31, faixas que o pin de
# 1.5.3 acima satisfaz — nenhuma das duas obriga a subir o core.
# langchain-postgres traz pgvector, sqlalchemy e asyncpg como dependencias.
langchain-postgres==0.0.17
langchain-text-splitters==1.1.2
